import React from "react";
import "./Pane.css";
import { defaultProps, propsTypes } from "./Pane.props";

const Pane = (props: propsTypes) => {
    return (<></>);
};

Pane.defaultProps = defaultProps;

export default Pane;