import React from "react";
import "./Item.css";
import { defaultProps, propsTypes } from "./Item.props";

const Item = (props: propsTypes) => {
    return (<></>);
};

Item.defaultProps = defaultProps;

export default Item;